# 改进方向

> 记录时间：2026-09-19
> 结论分两类，请分开看：**【已实测】**是我跑过一遍、有输出证据的；**【读码推断】**是我只读代码得出的，没跑过。

---

## 一、优先级总表

| # | 问题 | 严重度 | 状态 |
|---|---|---|---|
| B1 | 后天下发的动作不落地（关灯只是其中一个症状） | 高，功能等于没通 | 已实测，根因待查 |
| B2 | 白天「关灯」本来就没有任何视觉效果 | 高，用户一定会当成 bug | 已实测 |
| B3 | 多开标签页会互抢事件和记忆 | 中 | 读码推断 |
| B4 | 自主活动每 7 秒打一次 LLM，烧钱 | 中 | 读码推断 |
| B5 | 没有 git，且没有 .gitignore（`.env` 会泄漏） | 高，但好修 | 已实测 |
| M1 | 记忆只有 12 轮、进程内、重启即失忆 | 中 | 已实测 |
| D1 | 默认就「开着」的设备，打开命令是空操作 | 低 | 读码推断 |
| P1 | 换场景：后端基本能复用，前端房间相关的三张表要重写 | 信息 | 读码推断 |

---

## 二、B1 —— 后天下发的动作不落地【已实测】

### 现象

这是「关灯不能用」的真正原因，而且**不止关灯，是整条下发链路都没生效**。

实测（我直接打 `/chat` 接口，然后轮询 `/health` 看设备状态）：

| 我说 | LLM 返回的 actions | 预期 | 实际（观察 8~12 秒） |
|---|---|---|---|
| 把电灯打开 | `lightSwitch` / `on` | lightSwitch→1 | **一直 0** |
| 把窗帘拉下来 | `blinds` / `off` | blinds→0 | **一直 1** |
| 打开书柜门 | `cabinet` / `on` | cabinet→1 | **一直 0** |

三条都是稳态不变，不是延迟问题（我每 2 秒采样一次，采了 4~6 次）。

对照组：`door` / `fan` / `globe` 三条也在同一批测试里返回了 `op:"on"` 而状态没变 —— 但这三个**本来就处于「开」的状态**，所以它们不能作为反例，也不能作为证据。真正能说明问题的是上面那三条：`cabinet` 从 0 开不起来、`blinds` 从 1 关不掉。

### 已经排除的

- **不是 LLM 的问题**：它返回的 JSON 完全正确，`{"device":"lightSwitch","op":"on"}` 就是我要的。
- **不是设备名对不上**：我把 `room.html` 里全部 20 个 `bold:` 和 `DEV` 表的 19 项逐个对了，**每一个都能反查到 item**（`doorG`/`sashL`/`blindG`/`drawerG`/`lampG`/`mugG`/`chairG`/`cabL`/`bookG`/`clockG`/`switchG`/`sofaG`/`playG`/`picG`/`globeG`/`fanRoot`/`mobG`/`plantG`/`ballG`）。所以不存在「某个设备的 `byBold()` 返回 undefined 导致静默失败」。
- **不是 `Room.exec` 的逻辑错**：`room.html:825-835` 的 `on/off/toggle` 分支逻辑是对的，`devOn`（`room.html:599`）对 lightSwitch 读 `it.target`，而开关的 action（`room.html:446`）里 `it.target=lightState.switchOn?1:0` 是同步的，两边一致，没有错位。

### 剩下的怀疑方向（按可能性排序）

1. **浏览器标签页里跑的是旧代码。** 这个标签页从我开始改 room.html 到现在一直开着，中间我重启过服务。先做这一步：**Ctrl + F5 硬刷新**，再测一次。成本最低，先试这个。
2. **`room.html` 的 `stepAgent`（`room.html:766`）在某处抛异常**，导致 `Room.exec(job.name, job.op)`（`room.html:772`）根本没被执行到。如果「进去之后立刻返回 1」这类调子也能播，说明服务是活着的，看控制台（F12 → Console）一眼就知道。
3. **任务队列被自主活动覆盖。** `Agent.use`（`room.html:755`）第一句就是 `queue.length=0`，`applyEvent` 里的 `{type:'use'}`（`room.html:870`）也走同一个函数。所以只要轮询在这条命令执行完之前塞进来一个 `use` 事件，**用户刚下的命令会被自己的自发行为静默取消**。服务端的 `IDLE_GAP_SEC=10` 本来是为了挡这个的（说过话之后 10 秒内不自主活动），但事件是在服务端排队、浏览器什么时候拿到不一定，这个保护不是硬保证。

> 我没法开浏览器、也没有控制台，所以根因定不下来。**第一步请先硬刷新**；如果还是不行，F12 控制台把红字发我。

---

## 三、B2 —— 白天「关灯」本来就没有视觉效果【已实测】

这个和 B1 是两回事，就算 B1 修好了，白天关灯还是「看起来没反应」。

`room.html:947`：

```js
const roomNight = lightState.night && !lightState.switchOn;
```

- `lightState.night` = 当前时间是否 19:00–7:00（`room.html:946`）
- 白天时 `night=false`，于是 `roomNight` **恒为 false**，跟开关状态无关

也就是说：**白天点开关，房间不会发生任何变化**。开关只在夜里 19:00 之后才有意义（夜里开灯 = 房间翻回白底黑线，关灯 = 黑底白线）。

用户在白天的直观感受就是「关灯功能坏了」。实测时是 17:45，正好在白天，所以那个时间段测开关必定看起来是坏的。

**建议三选一：**
- 让开关在白天也有可见反馈（比如天花板上加一个线条画的吸顶灯，开了给个发光锥）
- 白天把开关从「可交互」里拿掉，并在对话框里明确告诉用户「现在天还亮，晚上再试」
- 重新定义「关灯」= 拉窗帘，让它变成一个白天也有效果的动作

---

## 四、B3 —— 多标签页互抢【读码推断】

`server/main.py:66` 是 `SESSION = Session()`，一个**全局单例**。没有 session id、没有 cookie。

后果：开两个标签页，它们
- 共用同一份 `room` / `agent` / `events`
- 共用同一个 `HISTORY`（对话记忆）
- 抢同一个 `cursor`：A 页把 `cursor` 推到 10 之后，B 页再拉 `/events?since=X` 就拿不到 1–10 之间的事件了，**B 页会丢事件**（比如小人不动）

单人单页没问题，但「我开两个窗口对比一下」就会出怪现象。

**建议：** 要么接受并写进文档（只支持一个标签页）；要么给 `/events` 加一个 `client` 参数，每个客户端维护自己的游标。

---

## 五、B4 —— 自主活动的 LLM 成本【读码推断】

`server/main.py:74` 的后台循环每 `IDLE_TICK_SEC`（默认 **7 秒**）跑一次 `run_tick`，而 `agent.py` 的 tick 分支里有几条路径会调 LLM（`mutter` / `react`）。

只要页面开着，**一小时最多约 500 次 LLM 调用**，即使没人在跟它说话。开着不管、挂一晚上，账单会很难看。

**建议：**
- 把自主活动分成两档：`mutter`/`react` 这类「说话」的走 LLM，但**加一个最小间隔**（比如 45~60 秒才允许一次 LLM 嘟囔，中间的 tick 只做「走过去动一下」这种不花钱的动作）
- 把 `IDLE_TICK_SEC` 默认值从 7 提到 15~20
- 或者干脆给自主活动也配规则大脑，只有用户主动说话时才用 LLM

---

## 六、B5 + M1 —— git 和记忆

### B5：没有 git，也没有 .gitignore【已实测】

```
git rev-parse --is-inside-work-tree  →  fatal: not a git repository
ls .gitignore                        →  No such file or directory
ls server/.gitignore                 →  No such file or directory
```

所以现在 `git init && git add .` 会直接提交两样不该提交的东西：

1. **`server/.venv/`** —— 几百 MB，clone 的人还得自己重建
2. **`server/.env`** —— **里面有你的 API key。推到 GitHub 就是公开泄漏**

第二个比第一个严重得多，而且是不可逆的（推到公开仓库后只能作废重建 key）。

**需要加的最小 `.gitignore`：**

```gitignore
# 密钥，绝对不能提交
server/.env
.env

# 虚拟环境
server/.venv/
.venv/
__pycache__/
*.pyc

# 系统
.DS_Store
Thumbs.db
```

**顺序注意：** 一定要**先写好 `.gitignore` 再 `git init` + 第一次 `git add`**。如果 `.env` 已经被 `git add` 过，光加 `.gitignore` 是没用的（已跟踪的文件不受影响），得用 `git rm --cached server/.env` 才能真正移出索引 —— 而且如果那时已经 push 过，唯一的补救是**去平台把 API key 吊销重建**。

### M1：记忆太短【已实测】

`server/agent.py:31`：

```python
HISTORY: deque = deque(maxlen=12)  # (role, text)，只记聊天，不记自主行为
```

四个限制：

1. **只有 12 轮**（问 + 答算 2 轮，所以实际只记得最近 6 次对话）
2. **进程内存**，服务一重启就全没了，没有任何持久化
3. **不记自主行为**：小人自己嘟囔的那句 `mutter` 不进历史，所以 LLM 不知道「我刚才说过什么」，串起来会显得前言不搭后语
4. **全局共享**，不区分用户/标签页

**建议（按投入从小到大）：**
- 先把 `maxlen` 提到 40~60，并让 `mutter` 的输出也进历史（改 `agent.py:96-97` 附近）
- 再把历史落盘：一张简单的 `history.jsonl` 或 SQLite 表，按 `timestamp + role + text` 存，启动时读回最近 N 条
- 如果要做「真的记得住」，那才需要向量检索 + 摘要；但对这个场景，**落盘 + 提高轮数已经能解决 95% 的体感问题**，不必上 RAG

---

## 七、我自己扫出来的其他问题

### D1：默认就「开着」的设备，打开命令是空操作【读码推断】

`devOn`（`room.html:599`）是「读目标状态」，但有几个设备**一加载就是「开」的目标值**：

| 设备 | 行 | 初始值 | `devOn` 启动即返回 |
|---|---|---|---|
| `mobile`（风铃） | `room.html:472` | `tv:0.9` | 1 |
| `plant`（盆栽） | `room.html:476` | `p:1,target:1` | 1 |
| `mug`（马克杯） | `room.html:405` | `p:1,target:1` | 1 |
| `clock`（挂钟） | `room.html:429` | `p:1,target:1` | 1 |

于是对它们说「打开风铃」「让盆栽动一下」，`Room.exec` 里 `op==='on'` 分支发现 `on` 已经是真值，**直接跳过，什么都不做**。用户体感就是「说了没用」。

（顺带：`room.html:468` 的吊扇和 `:464` 的地球仪**是** `tv:0` 起手的，所以它们本身没这问题。）

**建议：** 这几个「环境类」设备本来的语义就是「一直在动」，那就不该进 `devices.py` 的可操控白名单 —— 从清单里去掉，LLM 也就不会再对它们下无效指令。要么就给 `devOn` 换一个真实读当前动画状态（读 `it.v` 而不是 `it.tv`）的口径。

### 其他小项

- **LLM 会下发明知无效的动作。** 实测里它看到了房间状态是「吊扇=开着」，回了句「诶电扇本来就转着呀」，**但 actions 里还是给了 `{fan, on}`**。提示词（`server/brain.py:34-38`）里只说了「没有动作时给空数组」，没说「如果你想说的意思是它已经开着，就不要给动作」。改一句提示词就能省掉一批无效动作。
- **`/chat` 和 `/events` 没有任何鉴权。** `uvicorn` 默认只绑 `127.0.0.1`（本机），所以目前是安全的；但如果哪天加 `--host 0.0.0.0`，同一局域网里任何人都能驱动机器人、烧你的 key。
- **`updateLight()` 里有两套配色口径。** `room.html:950-952`、`954` 用的是 `roomNight`，而 `953`、`955-956`（`BOLDMAT_OUT` / `OUTFILL` / `OUTMAT`）用的是 `lightState.night`。也就是说夜里开了灯，房间翻成白底，但这一组材质**仍然是夜间配色**。我判断这是**故意的**（「窗外」的东西应该在开了室内灯之后依然保持夜色），但读代码看不出来，建议你确认一下是不是本意 —— 如果哪天有人「顺手统一」成 `roomNight`，窗外会跟着变白，那就错了。
- **`main.py:146` 把首页写死成 `room.html`**，见下一节。

---

## 八、换一个场景要改什么【读码推断】

直接回答：**后端基本不用重写；前端的「小人 + 导航 + 聊天框」是通用的，但房间相关的三张表必须为新场景重写。**

### 耦合面只有这四处

| 契约 | 位置 | 换场景要做什么 |
|---|---|---|
| 设备名清单 + 中文别名 | `server/devices.py` 的 `DEVICES` | **重写**，换成新房间的设备 |
| `/events` 事件格式 | `{type: go\|use\|emote\|say, ...}` | 不用改 |
| `/state` 上报格式 | `{room:{...}, agent:{x,z,rot,at}}` | 不用改 |
| 首页文件名 | `server/main.py:146` 写死 `room.html` | 改一行（或者做成可配） |

所以：**`agent.py`（LangGraph 行为图）、`brain.py`（双大脑 + 提示词）、`main.py`（轮询服务）这三份代码，换场景时可以原样复用。** 也就是说「换个房间」不需要重写后端。

### 但前端不是「适配所有」

`room.html` 里的 agent 部分是**用这个房间的坐标写死的**，这三张表必须为新场景重做：

1. **`obstacles()`（`room.html:615-628`）** —— 手写的家具占位矩形，坐标是这个房间的。新房间要重新量。
2. **`STAND` / `NICE`（`room.html:602-612`）** —— 19 个设备各自「站哪儿」，以及站那儿时对话框显示的中文地名（「窗边」「书柜前」）。纯手工。
3. **`DEV` 反查表（`room.html:578-597`）** —— 靠 `byBold()` 拿 `bold:` 组反查 item。**这要求新房间每一个可交互物件都必须有一个 `bold:` 组，而且名字要稳定。** 原房间刚好满足（20 个 item 全部有 `bold:`），但这是这个项目的巧合，不是通用约定。

另外两个隐含假设：
- `GRID_MIN=-5.7 / GRID_N=46 / CELL=0.25`（`room.html:631`）假设房间约 **11.4 × 11.4**。房间尺寸一变，这套网格要重算。
- 小人身高按 1.75 单位做的。原房间的门是 4.35 单位高，所以这是个**约 2.5 倍人**的夸张尺度。新房间如果比例不同，会显得小人特别大或特别小。

### 是通用的部分

A\* 寻路、拉直（string-pulling）、转向平滑、到位减速、走路循环、表情动作、气泡定位、聊天框样式与日夜反相 —— 这些**跟具体房间无关，可以照搬**。

### 建议

现在这套代码的正确切法是把前端拆成两层：

```
agent.js          ← 通用：寻路 / 走停 / 表情 / 聊天框 / 轮询客户端
scene.js          ← 每个房间一份：{ 边界, obstacles(), devices:{名:{bold, stand, nice, kind}} }
```

`agent.js` 只认那个场景描述表，不认「书柜」「吊扇」这种具体东西。这样下一个房间你只需要写一份场景表 —— 也就是把上面那三张表抽出来而已，**改动量不大，但现在不做的话，第二个房间就会变成复制粘贴一整个 room.html 然后开始漂移。**

---

## 九、建议的动手顺序

1. **先硬刷新浏览器再测一次**（排除 B1 的「旧页面」可能）。如果还不行，抓 F12 控制台报错。
2. **写 `.gitignore`，然后才 `git init`**（B5）。这件事越早做越好，因为一旦 key 推上去就晚了。
3. **修 B2（白天开关）**：不管 B1 结论如何，这个一定是用户能直接感知到的坑。
4. **修 B1 的根因**：确认后改。
5. **记忆落盘 + 提高轮数**（M1）。
6. **限制自主活动的 LLM 调用频率**（B4）—— 你现在配的是真 key，这个会直接花钱。
7. **把 agent 层抽成 `agent.js` + 场景表**（P1），为第二个房间做准备。
